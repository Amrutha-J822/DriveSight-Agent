fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
opencv-python-headless==4.10.0.84
numpy==2.2.1
httpx==0.28.1
python-dotenv==1.0.1

# Optional: install ultralytics locally if you want YOLO detection.
# It pulls in PyTorch (~500 MB) so it is not installed by default — Render's
# free tier only has 512 MB RAM and would OOM. Without it the backend falls
# back to OpenCV color/shape heuristics automatically.
#
# pip install ultralytics==8.4.52
#
# Then download a model (e.g. yolov8n.pt) and set YOLO_MODEL_PATH to its path.
